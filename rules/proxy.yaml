# ============================================
# 🔀 Proxy — домены через прокси
# ============================================

payload:
  # Разное
  - +.rule34.xxx              # hehe
  - +.e-hentai.org            # hehe
  - +.rule34video.com         # hehe
  - +.rule34video.co          # hehe (зеркало)
  - +.cobalt.tools            # Cobalt downloader
  - +.githubusercontent.com   # GitHub raw-контент
  - +.jsdelivr.net            # jsDeliver
  - +.notion.com              # Notion
  - +.notion.so               # Notion (зеркало)